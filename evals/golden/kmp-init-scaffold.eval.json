{
  "id": "kmp-init-scaffold",
  "skill": "kmp-init",
  "expected": {
    "must_do": [
      "collect exactly two inputs (project name, dotted package) plus an optional destination, and confirm the destination is empty or absent before writing",
      "run `kmp-ai-kit new` (dry-run first) rather than authoring any Gradle or Kotlin file by hand",
      "produce a self-contained project: the kit runtime is copied in and `opencode.json` references no path outside the project",
      "verify the scaffold: python3 shared/scripts/kmp_check.py --all passes with 0 features, e2e-check is 10/10, no template identifier (dev.kmpapp, KmpApp) remains, and the kit's absolute path appears nowhere",
      "end by handing off to /opsx-propose and kmp-create-feature instead of building a feature"
    ],
    "must_not": [
      "hand-write settings.gradle.kts, build.gradle.kts, module trees or Kotlin sources",
      "scaffold into the kit repo itself, or into a non-empty directory without explicit confirmation",
      "hand-edit the generated-resources import (it is derived from rootProject.name)",
      "leave an absolute path to the kit anywhere in the project (portability is the point of copying)",
      "start a feature build in the same run (that is kmp-create-feature's spec/design-gated workflow)"
    ]
  },
  "pass_criteria": "A runnable, self-contained project tree exists at the destination with no reference to any path outside itself; the checker passes with zero features; template identifiers are gone; the run stops at the handoff to OpenSpec.",
  "triggers": {
    "should": ["create a KMP app", "new Kotlin Multiplatform project", "scaffold a KMP starter", "kmp-ai-kit new GithubLeaderboard com.example.demo"],
    "should_not": ["add a feature to this app", "review the dashboard feature", "design this screen in Penpot"]
  }
}
